import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
           
            viewTest()
                           
                           .edgesIgnoringSafeArea(.all)
                           .border(.black)
        }
    }
}
